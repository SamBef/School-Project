# Base URL of the API (no trailing slash). Required for the app to reach the backend.
# Local: copy this file to .env and run the API (npm run dev in apps/api) on the same port.
# Production (Vercel): set in Vercel → Settings → Environment Variables.
VITE_API_URL=http://localhost:3003
