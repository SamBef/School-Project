# API base URL. In development with Vite proxy, leave empty to use /api and /admin proxy.
# For production, set to your API origin (e.g. https://api.yourdomain.com).
# VITE_API_URL="http://localhost:3000"
