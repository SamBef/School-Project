{
  "name": "kobotrack",
  "version": "0.1.0",
  "private": true,
  "description": "SME Transaction & Receipt Manager — Transaction tracking and receipt automation for microenterprises",
  "author": "",
  "license": "UNLICENSED",
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web & npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web:run": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web && npm run build --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "db:generate": "npm run db:generate --workspace=apps/api",
    "db:push": "npm run db:push --workspace=apps/api",
    "db:migrate": "npm run db:migrate --workspace=apps/api"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "overrides": {
    "vite": "5.0.12"
  }
}
